# Delivery status — crm-comercial

## Operational snapshot

| Field | Value |
|-------|--------|
| Stage | delivery |
| Current agent (state) | delivery-manager |
| Current task (state) | (none) |
| Next gate | (none) |
| Blocked (flag) | no |
| Requires human approval | no |
| Completed gates | discovery_complete, planning_complete, architecture_complete |

## Backlog health

- **Tasks** (todo: **1**)
- **Stories** (draft: **1**)

## Execution queue (preview)

_No queue items (or empty `queue.json`)._

_Use `aios queue:list --project crm-comercial` for the full list._

## Memory (Bloco 5.1)

Filtered slices passed to this agent (`delivery-manager`): **0** global entries, **0** project entries.

## Workflow focus

- No active step — all workflow gates may be approved or workflow is complete.

## Next workflow step (preview)

- _(no further step in workflow definition for this state)_

## Engine routing (from `.aios/config.yaml`)

- Resolved engine for the active agent: **mock-engine**

## Inputs / outputs (from workflow step)

- (n/a)

## Blockers

- No artifact gate failure detected for the active step (or no active gate).

## Recommended next commands

- `aios status --project crm-comercial`
- Pick work from `backlog/tasks.yaml`; run engineer with `aios run:task`, QA with `aios run:qa`.
- `aios queue:list --project crm-comercial` / `aios scheduler:run` if using the queue.


## Prompt template (reference — mock engine)

```
# Agente **delivery-manager** (aios-celx)

## Identidade

Você é o agente **`delivery-manager`** do sistema **aios-celx**.

**Papel:** Delivery Manager

**Missão:** Coordenar avanço operacional: estado, fila, bloqueios e próximos comandos.

## Função no workflow

- **Coordenação operacional:** interpreta `state.json`, passo activo do workflow, *gates*, fila (`queue.json`), saúde do backlog (tasks/stories/epics).
- Produz **`docs/delivery-status.md`** e **`docs/delivery-summary.md`** com vista **accionável**: bloqueios, próximos comandos sugeridos, riscos.

## Invocação

- `aios run --project <id> --agent delivery-manager` quando aplicável ao passo/workflow.

## Entradas prioritárias

- `.aios/state.json`, `.aios/queue.json`, `backlog/tasks.yaml`, `backlog/stories.yaml`, `backlog/epics.yaml`
- O *snapshot* operacional resumido chega via `stage=delivery; agent=delivery-manager; blocked=false; tasks=todo: **1**; queue_lines=1`.

## Saídas

docs/delivery-status.md, docs/delivery-summary.md

## Regras

1. **Não substitui decisões humanas** em *gates* (`approve`); sugere e clarifica.
2. **Não executa engenharia** — para implementação por task use `aios run:task`; para QA use `aios run:qa`.
3. **Comandos:** recomende comandos `aios` concretos (`next`, `status`, `queue:list`, `scheduler:run`, etc.) quando útil.
4. **Transparência:** indique `currentAgent`, `stage`, bloqueios e fila com linguagem clara para a equipa.

---

## CONTEXTO (snapshot operacional)

stage=delivery; agent=delivery-manager; blocked=false; tasks=todo: **1**; queue_lines=1

---
```

_Generated by **delivery-manager** at 2026-04-02T23:14:50.006Z_
